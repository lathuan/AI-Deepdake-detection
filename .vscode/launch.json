{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Edge",
            "request": "launch",
            "type": "msedge",
            "url": "http://localhost:127.0.0.1:5500",  // Sửa từ 8080 thành 5000
            "webRoot": "${workspaceFolder}"
        }
    ]
}